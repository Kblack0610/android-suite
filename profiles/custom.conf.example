# Custom Profile Template
# Copy this file to custom.conf and modify as needed

# =============================================================================
# Profile Metadata
# =============================================================================
PROFILE_NAME="custom"
PROFILE_DESCRIPTION="Your custom profile description"

# =============================================================================
# Animation Settings
# =============================================================================
# Scale: 0=off, 0.5=fast, 1.0=normal, 1.5+=slow
ANIMATION_SCALE=0.5

# =============================================================================
# Display Settings
# =============================================================================
# 1=light mode, 2=dark mode
DARK_MODE=1

# Screen timeout in milliseconds
# Common values: 30000 (30s), 60000 (1m), 120000 (2m), 300000 (5m)
SCREEN_TIMEOUT=120000

# =============================================================================
# Power Settings
# =============================================================================
# Enable aggressive doze mode (may delay notifications)
# 0=disabled (recommended), 1=enabled
AGGRESSIVE_DOZE=0

# Battery saver trigger percentage (5-50)
BATTERY_SAVER_LEVEL=20

# =============================================================================
# Developer Settings
# =============================================================================
# Keep screen on while charging
# 0=off, 1=AC only, 2=USB only, 3=AC+USB, 7=all (including wireless)
STAY_AWAKE_CHARGING=1

# =============================================================================
# Debloat Settings
# =============================================================================
# Also remove Google apps during debloat (be careful!)
# 0=keep Google apps, 1=remove Google apps
DEBLOAT_GOOGLE=0

# =============================================================================
# Custom Settings Function
# Add your own ADB settings commands here
# =============================================================================
apply_custom_settings() {
    log_info "Applying custom settings..."

    # Example: Disable haptic feedback
    # if ! is_dry_run; then
    #     setting_put system haptic_feedback_enabled 0
    #     log_success "Disabled haptic feedback"
    # fi

    # Example: Set custom keyboard
    # if ! is_dry_run; then
    #     setting_put secure default_input_method \
    #         "com.google.android.inputmethod.latin/com.android.inputmethod.latin.LatinIME"
    #     log_success "Set Gboard as default keyboard"
    # fi

    # Example: Grant permission to an app
    # if ! is_dry_run; then
    #     adb_cmd shell pm grant com.termux android.permission.WRITE_SECURE_SETTINGS
    #     log_success "Granted WRITE_SECURE_SETTINGS to Termux"
    # fi

    log_info "Add your custom settings to this function"
}
